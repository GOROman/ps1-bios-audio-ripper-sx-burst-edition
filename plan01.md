# plan01 — OFDM デコード精度の改善

## 現状の把握

- **ADPCM は既に最良**: OFDM データ経路（`src/ofdm_tx.c:92`）は `sx_spu_adpcm_encode_block`
  （5 フィルタ × shift 全探索・予測付き）を使用。`src/audio_tx.c` の粗い
  `emit_adpcm_block`（filter 0 / shift 1 固定）は **FSK トーン専用**で
  高忠実度は不要。`tests/spu_adpcm_test.c` は SNR > 20 dB をアサート。
  → **4bit SPU ADPCM の理論上限（≈18〜22 dB）にほぼ到達済み**。エンコーダ
  差し替えでは伸びない。
- **QPSK 復調の所要 SNR ≈ 10〜13 dB**。現状の余裕は 5〜8 dB しかなく、
  クロックドリフト・レベル誤差・CPU 起因のリサンプル誤差が乗ると CRC 落ち。

## モード別の前提（重要）

| モード | L/R の使い方 | 精度の効き所 |
|---|---|---|
| **ステレオ** | L と R は**別データ**（`channelCodes[0]`=下位2bit / `[1]`=上位2bit、2× スループット） | L+R 合成は**不可**。チャネル推定・軟判定・PAPR |
| **モノ** | L と R は**同じ信号** | **L+R 最大比合成 (MRC)** が使える（最大 +3 dB） |

`decodeMonoRobust` は現在「良い方のチャネルを選ぶ」だけ。

---

## 施策一覧（影響度・実装コスト付き）

### 受信側のみ（PS1 再ビルド不要）

#### ★A パイロット補間チャネル推定 — 最優先
- **現状**: 8 パイロットの位相を線形フィット（intercept + slope）＋
  スカラー gain（`median(pilot magnitudes)`）。
- **変更**: 8 点の複素パイロット値 `P[k]` を 96 bin へ補間して複素チャネル
  `H[k]` を作り、`Z[k] / H[k]` で **位相＋振幅**を等化。
  - 補間は線形（実部・虚部を別々に）か 2 次スプライン。
  - 端キャリア（bin 24 / 119）の減衰・位相ズレを吸収 → QPSK 判定マージン改善。
- **効果**: 全条件で効く。周波数選択性（スピーカー/マイク/ADC ロールオフ）が
  あるほど効く。**両モードに効く**。
- **対象**: `web/ofdm-worker.js` の `decode()` / `decodeMono()` の等化ループ、
  および WASM 版 `src/ofdm_demod.c`（両方直す＋ WASM 再ビルド）。
- **リスク**: 低。テスト WAV で回帰確認可能（`tests/ofdm_demod_test.c`）。

#### ★B モノ MRC（L+R 最大比合成）
- **変更**: モノ時、L と R それぞれ FFT → パイロットから `H_L[k]`, `H_R[k]` を
  推定 → `Ẑ[k] = (conj(H_L)·Z_L + conj(H_R)·Z_R) / (|H_L|² + |H_R|²)`。
- **効果**: 相関ノイズが無相関なら最大 **+3 dB**、チャネル固有ドロップアウトを平均化。
- **対象**: `decodeMonoRobust` を「良い方を選ぶ」から「合成」に。
- **前提**: ★A のチャネル推定が入っていること。
- **リスク**: 低〜中（モノ経路のみ）。

#### ★C 軟判定 + 内符号
- **変更**: QPSK 硬判定（`nearest`）をやめ、`Z[k]` と `|H[k]|` から LLR を作り、
  **畳み込み符号（K=7 等）or LDPC** の軟判定復号を内符号として追加。
  - 送信側にも符号化が必要（`src/ofdm_packet.c` / `ofdm_mod.c`）。
  - パケット CRC で丸ごと落ちている現状を、部分誤りから救済。
- **効果**: 最大の堅牢性向上。閾値 SNR を数 dB 下げられる。
- **対象**: 送受信両方＋ネゴ文字列（`E16+4` → 内符号パラメータ追加）。
- **リスク**: 大（プロトコル変更、両側改修、要広範テスト）。

### PS1 側（要再ビルド・Docker toolchain）

#### ★D PAPR ソフトクリッピング
- **変更**: `src/ofdm_mod.c` で合成 OFDM 波形を約 2.5σ でソフトクリップ →
  帯域内へ再フィルタ（クリップで出た帯域外成分を除去）。
  - クレストファクタ 12 dB → 8 dB 程度。
  - ADPCM の量子化ビットが「信号」に多く割り当たる → 実効 **+2〜4 dB**。
- **効果**: ★（ADPCM の頭打ちを間接的に緩和）。★A/★B と相乗。
- **対象**: `src/ofdm_mod.c`、`tests/ofdm_mod_test.c` の期待値更新、
  受信側の許容 EVM 確認。
- **リスク**: 中（波形が変わる＝受信閾値・相関に影響、要ループバックテスト）。

#### ★E 端キャリア BPSK
- **変更**: bin 24,25,118,119 など帯域端の数キャリアを QPSK → BPSK。
- **効果**: スループット微減（144B ペイロードが数バイト減）で端の誤りを潰す。
- **対象**: `src/ofdm_packet.c`（マッピング）、`ofdm_mod.c`、受信 demod、
  ペイロード長のネゴ。
- **リスク**: 中（ビット割り当ての整合）。

---

## 推奨手順

1. **★A パイロット補間チャネル推定** を実装（受信のみ、リスク低、全条件で効く）
2. `make analyze-ofdm` でエミュ録音を解析
   （per-carrier EVM / SNR vs bin / タイミングドリフト）
   → どの要因が支配的かを確定
3. モノ運用が主なら **★B MRC**、ステレオ主体なら **★D PAPR**
4. まだ足りなければ **★C 軟判定 FEC**

## 計測の指標
- per-carrier EVM（bin 24〜119）: 端で悪化 → ★A が効く
- SNR vs パケット番号: 単調悪化 → SRO（サンプルレートオフセット）追従が必要
- CRC 落ちの分布: バースト → FEC/インターリーブ、ランダム → SNR 底上げ

## 補足（今回は採用しない）
- **CP ベース微細同期（Schmidl-Cox）**: ケーブル接続では多重波ほぼ無く優先度低
- **Zadoff-Chu プリアンブル**: 相関ピーク先鋭化。効くが改修範囲が広い
- **16bit 再生**: SPU は任意 RAM データの 16bit 再生を持たない（CD-DA/XA も 4bit ADPCM）

## 2026-08-30 実施状況

- **★A 実装済み**: 同期シンボルから96キャリア個別の複素チャネル応答を推定し、
  `src/ofdm_demod.c` と `web/ofdm-worker.js` のステレオ／モノ復調へ適用。
- **★B 実装済み**: モノ復調はL/Rをチャネル電力で重み付けして合成。片側が無音の
  場合、そのチャンネルの寄与は実質ゼロになる。
- **モノをデフォルト化**: 片チャンネル入力へ対応するため、PS1起動時とWorkerの
  未指定時をモノに変更。明示されたステレオV2の復号互換性は維持。
- **静的・合成検証 PASS**: Workerのステレオ、モノ、片側無音モノ、および
  160パケットのSPU ADPCMループバックでCRC一致。
- **実機確認 PENDING**: 物理PlayStationの正常な片チャンネルからブラウザまでの
  復調成功は未確認。ビルドや合成ループバックを実機成功として扱わない。
- **★C〜★E 未実施**: 現行wire V2と理論実効レートは変更していない。
